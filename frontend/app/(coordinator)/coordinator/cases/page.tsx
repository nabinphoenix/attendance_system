"use client";
import{useEffect,useState}from"react";import Link from"next/link";import api from"@/lib/api";
export default function Page(){const[status,setStatus]=useState("open");const[rows,setRows]=useState<any[]>([]);async function load(){setRows((await api.get(`/api/v1/cases?status=${status}`)).data)}useEffect(()=>{load()},[status]);return <><h1 className="mb-6 text-3xl font-bold">Student case queue</h1><select value={status} onChange={e=>setStatus(e.target.value)} className="mb-5 bg-slate-800 p-2"><option value="open">Open</option><option value="in_progress">In progress</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select><table className="w-full"><thead><tr><th>Student</th><th>Trigger</th><th>Priority</th><th>Assigned</th></tr></thead><tbody>{rows.map(c=><tr key={c.id} className="border-t border-slate-800 text-center"><td><Link className="text-emerald-400" href={`/coordinator/cases/${c.id}`}>{c.student_id}</Link></td><td>{c.trigger_type}</td><td>{c.priority}</td><td>{c.assigned_to??"Unassigned"}</td></tr>)}</tbody></table></>}
