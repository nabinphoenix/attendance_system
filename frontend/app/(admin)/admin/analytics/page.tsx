"use client";
import{FormEvent,useState}from"react";import api from"@/lib/api";import{Button}from"@/components/ui/Button";
export default function Page(){const[section,setSection]=useState("1");const[batch,setBatch]=useState("1");const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[summary,setSummary]=useState<any>(null);const[candidates,setCandidates]=useState<any[]>([]);async function load(e:FormEvent){e.preventDefault();const[s,c]=await Promise.all([api.get(`/api/v1/analytics/sections/${section}/attendance-summary`),api.get(`/api/v1/analytics/selective-absence?date=${date}&batch_id=${batch}`)]);setSummary(s.data);setCandidates(c.data)}return <><h1 className="mb-6 text-3xl font-bold">Attendance analytics</h1><form onSubmit={load} className="flex flex-wrap gap-3"><input value={section} onChange={e=>setSection(e.target.value)} placeholder="Section ID" className="bg-slate-800 p-2"/><input value={batch} onChange={e=>setBatch(e.target.value)} placeholder="Batch ID" className="bg-slate-800 p-2"/><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="bg-slate-800 p-2"/><Button>Load</Button></form>{summary&&<section className="my-6"><h2 className="text-xl font-bold">Section average: {summary.overall_percentage}%</h2>{summary.students.map((s:any)=><p key={s.student_id}>{s.student_name}: {s.percentage}%</p>)}</section>}<h2 className="mt-8 text-xl font-bold">Selective-absence candidates</h2>{candidates.map(c=><div key={c.student_id} className="mt-2 rounded bg-slate-900 p-3">Student {c.student_id}: attended {c.attended_subjects.join(", ")} · missed {c.missed_subjects.join(", ")}</div>)}</>}
