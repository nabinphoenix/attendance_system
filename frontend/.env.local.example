# Leave this blank to use the same-origin /api proxy. This is required when
# accessing the app from a phone on the same Wi-Fi network.
NEXT_PUBLIC_API_URL=
# This is read only by Next.js on the development computer.
API_PROXY_TARGET=http://127.0.0.1:8000
